{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/web",
  "functions": {
    "server.cjs": {
      "runtime": "edge"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/server.cjs" },
    { "src": "/(.*)", "dest": "/server.cjs" }
  ]
}
