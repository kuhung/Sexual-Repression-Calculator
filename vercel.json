{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/web",
  "functions": {
    "server.cjs": {
      "runtime": "edge",
      "entrypoint": "server.cjs"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/server.cjs" },
    { "src": "/(.*)", "dest": "/server.cjs" }
  ]
}
